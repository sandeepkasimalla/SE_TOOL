Requirements summary:

Entending the future of Doxygen tool:

Doxygen will generate a document for given source code.

vizualizing the architecture of the source code by parsing the html file generated by Doxygen.

To run the Doxygen we have a Doxygen GUI frontend interface is there when we run this interface we will get html file.

after getting the file we have to parse the html file.

So we have to get the vizualised architecture of the html file when we run Doxygen itself.

This is the main Requirement.

For this We want sub requirements mentioned in Readme.
  

The requirements are python server, tools like xdotool, wmctrl.

For running the extension tool follow the steps in Readme.



